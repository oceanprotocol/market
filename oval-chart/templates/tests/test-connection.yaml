apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oval-chart.fullname" . }}-test-connection"
  labels:
    {{- include "oval-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oval-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
